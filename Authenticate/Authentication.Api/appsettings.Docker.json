{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Kestrel": {
    "Certificates": {
      "Default": {
        "Path": "/https/smartstay.pfx",
        "Password": "smartstay"
      }
    }
  },
  "Ntfy": {
    "BaseUrl": "https://ntfy:443"
  },
  "ConfirmEmail": {
    "BaseUrl": "${GATEWAY_PUBLIC_BASE_URL}",
    "PathPrefix": "${CONFIRM_EMAIL_PATH:-/api/auth}",
    "Suffix": "${CONFIRM_EMAIL_SUFFIX:-/ConfirmEmail}"
  },
  "ForgotPassword": {
    "BaseUrl": "${GATEWAY_PUBLIC_BASE_URL}",
    "PathPrefix": "${RESET_PASSWORD_PATH:-/api/auth}",
    "Suffix": "${RESET_PASSWORD_SUFFIX:-/ResetPassword}"
  }
}
