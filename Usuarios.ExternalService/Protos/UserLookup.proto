syntax = "proto3";

option csharp_namespace = "Authentication.Api.Protos";

package authentication;

// Service for user authentication operations
service UserLookup {
  // Get user ID by email address
  rpc GetUserIdByEmail (GetUserIdByEmailRequest) returns (GetUserIdByEmailResponse);
  // Get email by user ID
  rpc GetEmailByUserId (GetEmailByUserIdRequest) returns (GetEmailByUserIdResponse);
}

// Request message for getting user ID by email
message GetUserIdByEmailRequest {
  string email = 1;
}

// Response message containing the user ID
message GetUserIdByEmailResponse {
  string user_id = 1;
  bool found = 2;
}

// Request message for getting email by user ID
message GetEmailByUserIdRequest {
  string user_id = 1;
}

// Response message containing the email
message GetEmailByUserIdResponse {
  string email = 1;
  bool found = 2;
}
